
if (false) {

	var _iters = 1000;
	var _times = get_timer();

	var _math = 0, _it = 0;
	repeat _iters {
	
		_it += 1;
		if (true) {
		
			//if (true) {
			//	_math = _math * 24 / _math * power(_math, 4);
			//}
		
			if (!false) {
				_math = _math * 24 / _math * power(_math, 4);
			}
		
			//if (false) {
			//}
			//else {
			//	_math = _math * 24 / _math * power(_math, 4);	
			//}
		}
	}

	show_message((get_timer() - _times) / _iters);
}
